tsc -w 並開啟根目錄下的 index.html

處理 v-model雙向更新

3.reactive

#render方法都放置在
C:\Users\stual\OneDrive\桌面\read\vue3\write_code\packages\runtime-core\src\index.ts

#需要了解

(ts)
HostNode
HostElement
ElementNamespace
unique symbol
TConstructor
onst raw = observed && (observed as Target)[ReactiveFlags.RAW];
0 extends 1
T extends unknown
T extends CallExpression["callee"]

(js)
instanceof
Symbol.for('v-ndc')
Symbol.for("v-cmt")

(運算元)
if (cb.flags! & SchedulerJobFlags.ALLOW_RECURSE) {
cb.flags! &= ~SchedulerJobFlags.QUEUED
}

https://juejin.cn/post/7153889335232692255

# isBlock(如何與Block Tree互動)
# context.cache(目前不知道cache用在哪)
