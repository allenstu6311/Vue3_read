tsc -w 並開啟根目錄下的index.html
 
# 目前看到 Tokenizer stateBeforeAttrName


#需要了解

(ts)
ShadowRoot
HostNode
HostElement
ElementNamespace
unique symbol
TConstructor
onst raw = observed && (observed as Target)[ReactiveFlags.RAW];
0 extends 1
T extends unknown

(js)
instanceof
 Symbol.for('v-ndc')
 Symbol.for("v-cmt");

